# MCP server framework (FastMCP + CLI helpers)
mcp[cli]

# Terminal UI framework (Textual - modern TUI with better layout control)
textual

# gRPC client for RetrieveUserQuotaSummary via agy's local language server
grpcio

# YAML support for statusline.yaml
pyyaml

# HTTP client for background version checking
httpx

# Windows Credential Manager access (used by tui.py for OAuth profile email)
pywin32

